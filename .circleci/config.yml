version: 2.1
orbs:
  python: circleci/python@3.0.0
jobs:
  deploy-image:
    machine:
      image: ubuntu-2204:current

    steps:
    
      - checkout

      - run:
          name: "Deploy"
          command: |
            pip3 install cfstack tabulate
            python3 execute.py 


workflows:
  deploy-image: 
    jobs:
      - deploy-image